<div class="content">
  <div class="animated fadeIn">
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-header">
            <div class='row'>
              <div class="col-sm-4">
                <%= t('marketplace.title') %>
              </div>
              <div class="col-sm-8 text-right">
                <%= link_to new_marketplace_path, class: 'btn btn-sm btn-primary pull-right' do %>
                  <i class="fa fa-plus"></i> <%= t('add') %>
                <% end %>
              </div>
            </div>          
          </div>
          <div class="card-body">
            <div class="row">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th><%= t('name') %></th>
                    <th><%= t('url') %></th>
                  </tr>
                </thead>
                <tbody>
                  <% @collections.each do |marketplace| %>
                    <tr>
                      <td><%= marketplace.name %></td>
                      <td><%= marketplace.website_url %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="clearfix"></div>