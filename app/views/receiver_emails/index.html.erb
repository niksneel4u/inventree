<div class="row">
  <div class="col-lg-12">
    <div class="card">
      <div class="card-header">
        <div class='row'>
          <div class="col-sm-4">
            <%= t('receiver_email.title') %>
          </div>
          <div class="col-sm-8 text-right">
            <%= link_to new_receiver_email_path, class: 'btn btn-sm btn-primary pull-right' do %>
              <i class="fa fa-plus"></i> <%= t('add') %>
            <% end %>
          </div>
        </div>
      </div>
      <div class="card-body">
        <div class="collectionList row">
          <%= render 'list' %>
        </div>
      </div>
    </div>
  </div>
</div>
